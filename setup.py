from setuptools import setup

setup(
    name='GPIO_over_tcp',
    version='1.0.0',
    packages=['gpio_over_tcp'],
    url='',
    license='MIT',
    author='gpio_over_tcp@maslovw.com',
    author_email='',
    keywords='gpio tcp raspberrypi socat',
    description='Control GPIO over TCP (raspberrypi)'
)
